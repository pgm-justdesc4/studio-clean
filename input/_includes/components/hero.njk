{% macro hero(title, link) %}
    <div class="hero">
        <h1>{{title}}</h1>

        {% if link %}
        <div class="hero__btn">
            <a href="{{link.url}}">{{link.text}}</a>
        </div>
        {% endif %}
    </div>
{% endmacro %}