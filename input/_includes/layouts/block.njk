{% import "components/general.njk" as components %}

{% extends "layouts/default.njk" %}

{% block content %}
    <main>
        <section class="hero hero--btn">
        {{ components.hero(title, link) }}
        </section>
        <section class="spotlight-img">
            {{ components.spotlight(images)}}
        </section>

        {% block bottom %}
        {% endblock bottom %}
    </main>
{% endblock content %}